#include <iostream>
using namespace std;

int main(void)
{
    int a, b, c, d;
    cin >> a >> b >> c >> d;

    int diferenca = a * b - c * d;

    cout << "DIFERENCA = " << diferenca << '\n';

    return 0;
}